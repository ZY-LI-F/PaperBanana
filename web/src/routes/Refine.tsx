import { RoutePlaceholder } from './RoutePlaceholder';

export default function RefineRoute() {
  return <RoutePlaceholder description="Single-image edit parity with the legacy UI lands in T11." eyebrow="T11" title="Refine" />;
}
